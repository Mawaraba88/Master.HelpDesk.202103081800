
@model HelpDeskWeb.ViewModels.Commentaires.PostCommentViewModel
@using (Ajax.BeginForm("Create", "Commentaires",
        new AjaxOptions
        {
            HttpMethod = "POST",
            InsertionMode = InsertionMode.InsertBefore,
            UpdateTargetId = "commentaires"
        }))
{
@Html.AntiForgeryToken()

                @Html.HiddenFor(m => m.TicketID)
                                @Html.EditorFor(m => m.Contenu)

                                                <div class="col-md-5">
                                                    <input type="submit" class="btn btn-default pull-right" />
                                                </div>}
<br />
<br />
<br />